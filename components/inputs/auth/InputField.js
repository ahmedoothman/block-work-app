import { View, Text } from 'react-native';
import React from 'react';

const InputField = ({ title, placehodlder }) => {
  return (
    <View>
      <Text>InputField</Text>
    </View>
  );
};

export default InputField;
